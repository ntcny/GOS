
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%% настройка колонтитулов
%%%
	\renewcommand{\sectionmark}[1]{\markboth{%
		{\thesection\hspace{1em}#1}}% левая пометка
		{}% правая пометка (она пуста)
	}
	\newcounter{lections} 		% создадим новый счетчик, например для лекций
	\setcounter{lections}{0}	% пока лекций не было
	\newcommand{ \lection}[1]{	% опишем команду --- начало <<лекции>>
		\addtocounter{lections}{1}
		\textbf{
			\textcolor{red}{Лекция \arabic{lections}} :
		} \textcolor{blue} {#1}
	}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%% создание новых комад форматирования (by US Marine)
%%%
	\newcommand{\WorkHeader}[2]{\setcounter{section}{#1}\addtocounter{section}{-1}\section{#2}}
\newcommand{\TPHeader}[1]{\section*{#1}}
\newcommand{\WorkProblem}[1]{\par\textbf{Задача: }#1}
\newcommand{\kav}[1]{\textquotedblleft #1\textquotedblright}
\newcommand{\zagolovok}[1]{\par{\textit{\textbf{#1}}}}

\makeatletter
\newcommand{\mybox}{%
    \collectbox{%
        \setlength{\fboxsep}{1pt}%
        \fbox{\BOXCONTENT}%
    }%
}
\makeatother



\newcounter{theclaim} \setcounter{theclaim}{1}
\newenvironment{claim}[1][\unskip]{%
\textbf{Утверждение$\star$ \arabic{theclaim} (#1):\stepcounter{theclaim}}
\noindent }
{}

\newcounter{theproofs} \setcounter{theproofs}{1}
\newenvironment{proofs}[1][\unskip]{%
\textbf{Теорема$\bigstar$ \arabic{theproofs} (#1):\stepcounter{theproofs}}
\noindent }
{}

\newenvironment{dokvo}[1][\unskip]{%
\par
\textbf{Доказательство$\Box$}
\noindent }
{$\blacksquare$}

\newcommand{\vivod}[1]{\begin{center} \textbf{\textit{Вывод}}: #1 }
\newcommand{\skobk}[1]{\left( #1\right)}
\newcommand{\fskobk}[1]{\left\{ #1\right\}}
\newcommand{\DModul}[1]{\left|\left| #1\right|\right|}
\newcommand{\Modul}[1]{\left| #1\right|}
\newcommand{\predel}[1]{\lim\limits_{#1}}
\newcommand{\summa}[2]{\sum\limits_{#1}^{#2}}
\newcommand{\Real}{\mathbb{R}}
\newcommand{\Rmern}[1]{\mathbb{R}^#1}
\newcommand{\tittg}{\Leftrightarrow}
\newcommand{\sledue}{\Rightarrow}
\newcommand{\prokol}[1]{\mathring{#1}}
\newcommand{\myvect}[2]{\left(x_#1,\ldots,x_#2\right)}
\newcommand{\tochka}{\cdot}
\newcommand{\naverh}[1]{\stackrel{\text{#1}}}
\newcommand{\fx}{$f(x)$}
\newcommand{\obed}{\cup}
\newcommand{\peres}{\cap}
\makeatletter
\def\@seccntformat#1{%
  \expandafter\ifx\csname c@#1\endcsname\c@section\else
  \csname the#1\endcsname\quad
  \fi}
\makeatother

\makeatletter
\let\latexl@section\l@section
\def\l@section#1#2{\begingroup\let\numberline\@gobble\latexl@section{#1}{#2}\endgroup}
\makeatother
